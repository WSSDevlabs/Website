# PROGRESS

## 2026-08-09 — /memory init
- Initialized the memory/ structure for this project (PROJECT, CLIENT, TASKS, PROGRESS, DECISIONS, FINANCE stub).
- Confirmed: internal project (WSS DevLabs' own site), currently mid-redesign, target date 2026-08-12.
- Next step: scope the remaining redesign work into TASKS.md and continue with the appropriate role (/uiux or /dev depending on what's next).

## 2026-08-09 — /ceo pivot: Malaysian SME market + volume pricing
- Founder directive: pivot company focus to volume acquisition in the Malaysian SME/traditional-business market, price a Starter package as low as RM500, target quantity over per-project margin, and get /admin's client-document set fully standby.
- CMO research (web search, 2026-08-09): MSME Digital Grant MADANI (matching grant up to RM5,000, RM150M allocated under Budget 2026), Digital Acceleration Grant (RM53M for AI/blockchain/cybersecurity), National AI Office funding; Malaysia website market rates (freelancer RM1,500–6,000, agency RM3,000–25,000+); e-Invoicing exemption threshold raised to RM1M revenue as of 2026-01-01 (most micro-SMEs now exempt, so not used as an urgency lever).
- CEO call: rather than lowering the existing Launch/Build/Enterprise ladder (which would undo the prior session's "full digital solutions studio" repositioning), added a new SME tier underneath it — Starter RM500, Business RM1,500 — plus a monthly Care Plan add-on (RM99–199/mo) as the recurring-revenue engine that makes the low one-time price sustainable for a solo founder. Full rationale logged in DECISIONS.md.
- Shipped: `src/data/pricing.json`/`pricing.ts` updated with the two new tiers + `segment`/`note` fields; `/pricing` page rebuilt into two grouped sections (SME vs growth) with MADANI grant messaging and a Care Plan callout; FAQ updated with a grant question and revised turnaround answer. Verified with `astro check` (0 errors) and a direct HTTP fetch of the rendered page (HTTP 200, all tiers present) — browser automation (gstack browse) was blocked by a Windows Application Control policy on this machine, so visual QA was HTML-content-based rather than screenshot-based.
- Shipped (admin): 5 new client document templates — service-agreement (the actual contract), change-request, content-intake-form, care-plan-agreement, grant-assistance-info-sheet — plus a "standby document set" checklist added to `admin/SKILL.md` covering every client regardless of package size.
- Open/next: hero and services-page copy for the SME audience not yet updated (queued in TASKS.md); CMO campaign/ad copy execution not started (research only); all new templates use placeholder business details (SSM no., bank account) that need filling before first real client use; MSME grant figures should be verified against official sources before quoting, since they were sourced from web search rather than the primary government site.

## 2026-08-09 — "proceed everything": execution pass on the SME pivot
- Deeper grant research surfaced a correction: MADANI is a **50% match up to RM5,000**, not full/near-free coverage as the first pass implied — it's first-come-first-served against a fixed RM50M pool, one claim per business, and requires ≥6 months SSM registration plus ≥RM50,000 annual turnover (so brand-new hawker stalls may not qualify yet). Corrected the wording everywhere it appeared: `src/data/pricing.json` tier notes, `src/data/faq.json`, `grant-assistance-info-sheet.md`, `memory/FINANCE.md`. Added explicit per-tier math (Starter → client pays ~RM250 after match, Business → ~RM750) so /admin never oversells it as "free."
- Surfaced the existing `GrantSolutions.astro` component (already built, previously only shown on individual `/services/[slug]` pages, already had a "PMKS Madani Grant 2026 · SME Digital Upgrades" card) on the homepage instead of building a new banner from scratch — added `ctaHref` support and pointed the SME card at `/pricing`.
- **Found and fixed a real content-integrity issue while doing this work, unprompted:** the homepage, `/testimonials`, `/work`, every `/services/[slug]` page, and the unused `/coming-soon` page all hardcoded fabricated client testimonials (fake names like "Ahmad Razif," fake companies like "Nexora Sdn Bhd," fake stock-photo headshots via picsum.photos) as fallbacks, directly contradicting the project's own documented content gate (`README.md`: "no placeholder/fake trust content ships") and the empty `TESTIMONIALS` array in `src/data/testimonials.ts`, which even carried the comment "add real testimonials before this section ships." `/work` separately had 8 fabricated case studies with invented performance metrics (e.g. "5.2x ROAS," "cut cost-per-lead by 40%") as its empty-state fallback. Rewired all of these to the real (currently empty) data sources and replaced the fake fallbacks with honest "coming soon" empty states that point visitors to `/contact` or `/work` instead. This should be treated as a real prior mistake worth remembering, not just a stylistic cleanup — check `git log`/`git blame` on these files if it matters who introduced it.
- Wrote the CMO SME launch campaign at `docs/marketing/SME-LAUNCH-CAMPAIGN.md`: 3 Facebook/Instagram ad variants (Bahasa Malaysia, price-led / grant-led / credibility-led), a WhatsApp broadcast template, an organic launch post, and a 7-day launch sequence with a feedback loop back into `memory/FINANCE.md`. Flagged that it needs real WhatsApp/social handles before running (currently placeholders).
- Full verification this round: `astro check` clean (0 errors) after every batch of edits; live HTTP fetches of `/`, `/pricing`, `/work`, `/testimonials`, `/services/web-development` all returned 200 with zero fabricated names remaining and the corrected grant copy present. Browser automation (gstack browse) remains blocked by a Windows Application Control policy on this machine — noted again in case it's worth fixing for future visual QA.
- Still open: fuller hero/services-page copy rewrite for the SME audience (a lighter touch than originally planned, since the grant section + pricing page already carry the message); admin templates still need real SSM/bank/address details from the founder; campaign doc needs real business contact handles before going live.

## 2026-08-09 — CMS check + competitor-matched pricing overhaul
- Founder asked if the site has a CMS. Investigated and confirmed: yes, a live git-based CMS (Decap CMS) at `/wss-console`, backed by GitHub (`WSSDevlabs/Website`, editorial workflow) via a working Cloudflare Worker OAuth proxy (`wss-cms-auth.wssdevlabs.workers.dev`, confirmed live — returns 302 to GitHub auth). Covers 9 content types (projects, posts, services, pricing, team, career, faq, partners, site); testimonials is the one type not wired in. Also discovered while checking `site.json` that the real WhatsApp number and social handles are already filled in — only `legalName`/`ssmNumber`/`sstNumber` are genuinely blank, correcting an earlier note that assumed all business contact info was placeholder.
- Founder then shared 3 competitor URLs (websitedesign.swot.com.my, swot.com.my, swot.com.my/web-design-company) and asked for "similar package, price, item in package, and everything like them." Fetched all three and extracted SWOT's full pricing: 5 "Informative Website Packages" (RM1,799–RM3,499, by page count) and 4 "Ecommerce Website Packages" (RM3,899–RM6,799, by page count), each with specific feature sets (animated banners, CMS, SEO keyword counts, product catalogues, inventory/shipping/payment gateway for ecommerce, 8% SST exclusion, RM549/yr renewal).
- Flagged the conflict before building: SWOT's prices are far above this morning's RM500/RM1,500 quantity-focused tier. Asked the founder directly (undercut / match / layer above) rather than guessing on a real pricing decision. Founder chose: ship SWOT's exact prices now as placeholders ("I will change later"), package list is the priority; add Ecommerce as a new category (not folded into existing tiers).
- Shipped: replaced the Starter/Business tiers entirely with the 9-package SWOT-mirrored ladder (own copy/wording, not copied marketing text) in `src/data/pricing.json`; updated `pricing.ts`'s segment type to `'informative' | 'ecommerce' | 'growth'`; extracted the repeated card markup into a new `PricingCard.astro` component (was about to be duplicated a 3rd time); rebuilt `/pricing` into three grouped sections. Updated every place that referenced the old Starter/Business names or RM500/1,500 figures: `faq.json` turnaround answer, `GrantSolutions.astro`'s SME card copy, `grant-assistance-info-sheet.md`'s per-package grant math, and `memory/FINANCE.md`'s rate table.
- Logged this as an explicit **supersession** in `memory/DECISIONS.md` (not a silent overwrite) referencing the original RM500 decision, since the founder's own quantity-pricing call from this morning was being reversed by their own later instruction — kept both decisions visible with the reasoning for each.
- Flagged two things for later, not fixed now (out of scope of "ship the list first"): the new "10+ Pages" Informative package (RM3,499) overlaps in price/scope with the pre-existing "Launch" growth tier (from RM3,000) — unreconciled; and every SWOT-matched price is explicitly a placeholder the founder said they'd retune.
- Verified: `astro check` clean (0 errors), live HTTP fetch of `/pricing` confirmed all 9 package names and all 9 exact prices render, homepage still 200.

## 2026-08-09 — /cmo audit, then act on 2 of the findings
- Ran a CMO-lens audit of the whole site (not asked for by a specific role command until invoked as `/cmo`): checked for a positioning doc (none existed), proof content (still zero — same gap flagged earlier today), technical SEO basics (robots.txt/sitemap/JSON-LD all fine, but default OG image and self-hosted fonts are still empty per the content gate), and found two new issues by reading actual page code rather than just the rendered site: the footer newsletter signup form (`Footer.astro`) has `onsubmit="return false;"` and is completely non-functional — it silently discards every email address; and `/career` presented a fictional 3-person team, multiple open job positions, health insurance, and a "fully stocked kitchen at our KL workspace" despite every internal role doc describing WSS as a one-person company. Gave the founder a ranked list rather than fixing anything unprompted, since several of these (career page, positioning direction) are founder calls.
- Founder responded with two direct instructions: remove the career page, and build the positioning doc.
- **Career page removal:** deleted `src/pages/career.astro`, `src/data/career.json`, `src/data/career.ts`; removed both nav references in `src/data/nav.ts` (main nav "Company" group + footer company links); removed its collection from `public/wss-console/config.yml` (the CMS would otherwise still offer to edit a page that no longer exists). Verified `/career` now returns a clean 404 and grepped the full `src` tree to confirm zero remaining references. Logged as a decision (not just a task) since it was a real content/positioning call, not a bug fix.
- **Positioning doc:** auto-drafted `.agents/product-marketing.md` (the canonical location per the product-marketing skill) from the codebase — README, `pricing.json`, `services.json`, `site.json`, `about.astro`, the BizBrain role skill files (which state outright this is a one-person company), and this session's own history (SME pivot, SWOT-matched pricing, career removal). Covered the full template (overview, audience, competitive landscape vs. SWOT, differentiation, objections, switching dynamics, brand voice, proof points, goals) and added an extra "Open Issues" section — not in the skill's template, but worth keeping — that surfaces the three things a downstream marketing skill would otherwise get wrong if it just read the doc at face value: zero proof content, the About page's fictional team roster (found while drafting this — `about.astro` lists "Wan Syahiid," "Farah Izzati," and "Rin Takahara" as staff, same species of issue as the career page, not yet removed since the founder didn't ask for that one), and placeholder pricing. Added `memory/MARKETING.md` as the memory-system pointer to it, since `/cmo`'s own workflow checks that path first.
- Did NOT touch the About page team roster, the broken newsletter form, or any of the other audit findings beyond the two the founder explicitly named — all queued in `TASKS.md` with enough detail to act on later without re-deriving the reasoning.
- Verified: `astro check` clean (0 errors, file count dropped 79→77 matching the 2 deleted pages), dev server check confirmed `/career` 404s, `/` and `/about` still 200, footer no longer shows a Career link.

## 2026-08-10 — /memory recall found unlogged work, then a 20-item founder punch list

- Ran `/memory recall` at session start and found a large batch of **uncommitted, unlogged work** already sitting in the working tree (not from this session): 7 real blog posts added to `src/content/posts/` with honest `sourceUrl`/`sourceLabel` attribution, the About page's fictional 3-person team replaced with a real "Razali, Founder" placeholder frame, a fully-wired "Leave a review" modal on `/testimonials` (Web3Forms), a "Special Partnership Program" section, and reworked `contact.astro`/`faq.astro`/`insights` pages. Flagged the discrepancy to the founder rather than assuming it was mine or fully understanding its scope.
- Found and fixed the one real gap: the footer newsletter form (`Footer.astro`) still had `onsubmit="return false;"`, silently dropping every email. Wired it to Web3Forms using the same pattern as the other forms on the site (contact, hero quote, review modal), with loading/success/error states.
- Founder then sent a 20-item punch list (numbered, screenshot-referenced) covering homepage, contact, insights, about, testimonials, and FAQ pages, and asked to check what was already done vs. not. Read every relevant file's current (working-tree) state — Header, Hero, WhateverYouNeed, Differentiator, index, AboutSection, contact, about, faq, testimonials, insights/index, insights/[slug] — against each of the 20 asks.
- **Result: all 20 items were already implemented** in the uncommitted working tree (from the same earlier unlogged session), including some fairly involved builds: a hover-triggered navbar dropdown with a services grid box, the hero image replaced by a working free-quote form, homepage section reorder (grants block now sits directly above the FAQ teaser), the "Have a project in mind" CTA no longer rendered on the homepage, "Happy clients" already at 15+ (only one occurrence in the codebase, `Differentiator.astro`), the contact page's location card, subject field, and Reach-us relabel, and — the most elaborate one — an Insights click-to-open modal system: clicking a post title opens a popup with the full first-person article content plus an explicit "Visit the source" button, rather than navigating to a new page (individual `/insights/[slug]` pages still exist too, as an SEO-crawlable fallback).
- Verified every item against the *live rendered HTML*, not just the source: `astro check` (0 errors), dev server HTTP 200 on all 6 changed pages, and targeted string checks (`grep -c`/byte-offset order checks) confirming: "Have a project in mind", "Meet our expert team", "Discover More", "Still have questions", and "Call Center"/"X / Twitter" are all absent from their respective pages now; "Razali" and "Special Partnership Program" are present; the grant section's byte offset in the homepage HTML precedes the FAQ section's.
- Did not find anything to build from scratch for this punch list — the only code change this session was the newsletter form fix (see above). Told the founder plainly that most of the list was already done rather than re-implementing working features.
- Still open: none of this batch (blog posts, About/testimonials/contact/insights rework, newsletter form) is committed to git yet — it's all uncommitted working-tree state. Founder has not been asked yet whether to commit.

# PROGRESS

## 2026-08-09 — /memory init
- Initialized the memory/ structure for this project (PROJECT, CLIENT, TASKS, PROGRESS, DECISIONS, FINANCE stub).
- Confirmed: internal project (WSS DevLabs' own site), currently mid-redesign, target date 2026-08-12.
- Next step: scope the remaining redesign work into TASKS.md and continue with the appropriate role (/uiux or /dev depending on what's next).

## 2026-08-09 — /ceo pivot: Malaysian SME market + volume pricing
- Founder directive: pivot company focus to volume acquisition in the Malaysian SME/traditional-business market, price a Starter package as low as RM500, target quantity over per-project margin, and get /admin's client-document set fully standby.
- CMO research (web search, 2026-08-09): MSME Digital Grant MADANI (matching grant up to RM5,000, RM150M allocated under Budget 2026), Digital Acceleration Grant (RM53M for AI/blockchain/cybersecurity), National AI Office funding; Malaysia website market rates (freelancer RM1,500–6,000, agency RM3,000–25,000+); e-Invoicing exemption threshold raised to RM1M revenue as of 2026-01-01 (most micro-SMEs now exempt, so not used as an urgency lever).
- CEO call: rather than lowering the existing Launch/Build/Enterprise ladder (which would undo the prior session's "full digital solutions studio" repositioning), added a new SME tier underneath it — Starter RM500, Business RM1,500 — plus a monthly Care Plan add-on (RM99–199/mo) as the recurring-revenue engine that makes the low one-time price sustainable for a solo founder. Full rationale logged in DECISIONS.md.
- Shipped: `src/data/pricing.json`/`pricing.ts` updated with the two new tiers + `segment`/`note` fields; `/pricing` page rebuilt into two grouped sections (SME vs growth) with MADANI grant messaging and a Care Plan callout; FAQ updated with a grant question and revised turnaround answer. Verified with `astro check` (0 errors) and a direct HTTP fetch of the rendered page (HTTP 200, all tiers present) — browser automation (gstack browse) was blocked by a Windows Application Control policy on this machine, so visual QA was HTML-content-based rather than screenshot-based.
- Shipped (admin): 5 new client document templates — service-agreement (the actual contract), change-request, content-intake-form, care-plan-agreement, grant-assistance-info-sheet — plus a "standby document set" checklist added to `admin/SKILL.md` covering every client regardless of package size.
- Open/next: hero and services-page copy for the SME audience not yet updated (queued in TASKS.md); CMO campaign/ad copy execution not started (research only); all new templates use placeholder business details (SSM no., bank account) that need filling before first real client use; MSME grant figures should be verified against official sources before quoting, since they were sourced from web search rather than the primary government site.

## 2026-08-09 — "proceed everything": execution pass on the SME pivot
- Deeper grant research surfaced a correction: MADANI is a **50% match up to RM5,000**, not full/near-free coverage as the first pass implied — it's first-come-first-served against a fixed RM50M pool, one claim per business, and requires ≥6 months SSM registration plus ≥RM50,000 annual turnover (so brand-new hawker stalls may not qualify yet). Corrected the wording everywhere it appeared: `src/data/pricing.json` tier notes, `src/data/faq.json`, `grant-assistance-info-sheet.md`, `memory/FINANCE.md`. Added explicit per-tier math (Starter → client pays ~RM250 after match, Business → ~RM750) so /admin never oversells it as "free."
- Surfaced the existing `GrantSolutions.astro` component (already built, previously only shown on individual `/services/[slug]` pages, already had a "PMKS Madani Grant 2026 · SME Digital Upgrades" card) on the homepage instead of building a new banner from scratch — added `ctaHref` support and pointed the SME card at `/pricing`.
- **Found and fixed a real content-integrity issue while doing this work, unprompted:** the homepage, `/testimonials`, `/work`, every `/services/[slug]` page, and the unused `/coming-soon` page all hardcoded fabricated client testimonials (fake names like "Ahmad Razif," fake companies like "Nexora Sdn Bhd," fake stock-photo headshots via picsum.photos) as fallbacks, directly contradicting the project's own documented content gate (`README.md`: "no placeholder/fake trust content ships") and the empty `TESTIMONIALS` array in `src/data/testimonials.ts`, which even carried the comment "add real testimonials before this section ships." `/work` separately had 8 fabricated case studies with invented performance metrics (e.g. "5.2x ROAS," "cut cost-per-lead by 40%") as its empty-state fallback. Rewired all of these to the real (currently empty) data sources and replaced the fake fallbacks with honest "coming soon" empty states that point visitors to `/contact` or `/work` instead. This should be treated as a real prior mistake worth remembering, not just a stylistic cleanup — check `git log`/`git blame` on these files if it matters who introduced it.
- Wrote the CMO SME launch campaign at `docs/marketing/SME-LAUNCH-CAMPAIGN.md`: 3 Facebook/Instagram ad variants (Bahasa Malaysia, price-led / grant-led / credibility-led), a WhatsApp broadcast template, an organic launch post, and a 7-day launch sequence with a feedback loop back into `memory/FINANCE.md`. Flagged that it needs real WhatsApp/social handles before running (currently placeholders).
- Full verification this round: `astro check` clean (0 errors) after every batch of edits; live HTTP fetches of `/`, `/pricing`, `/work`, `/testimonials`, `/services/web-development` all returned 200 with zero fabricated names remaining and the corrected grant copy present. Browser automation (gstack browse) remains blocked by a Windows Application Control policy on this machine — noted again in case it's worth fixing for future visual QA.
- Still open: fuller hero/services-page copy rewrite for the SME audience (a lighter touch than originally planned, since the grant section + pricing page already carry the message); admin templates still need real SSM/bank/address details from the founder; campaign doc needs real business contact handles before going live.

## 2026-08-09 — CMS check + competitor-matched pricing overhaul
- Founder asked if the site has a CMS. Investigated and confirmed: yes, a live git-based CMS (Decap CMS) at `/wss-console`, backed by GitHub (`WSSDevlabs/Website`, editorial workflow) via a working Cloudflare Worker OAuth proxy (`wss-cms-auth.wssdevlabs.workers.dev`, confirmed live — returns 302 to GitHub auth). Covers 9 content types (projects, posts, services, pricing, team, career, faq, partners, site); testimonials is the one type not wired in. Also discovered while checking `site.json` that the real WhatsApp number and social handles are already filled in — only `legalName`/`ssmNumber`/`sstNumber` are genuinely blank, correcting an earlier note that assumed all business contact info was placeholder.
- Founder then shared 3 competitor URLs (websitedesign.swot.com.my, swot.com.my, swot.com.my/web-design-company) and asked for "similar package, price, item in package, and everything like them." Fetched all three and extracted SWOT's full pricing: 5 "Informative Website Packages" (RM1,799–RM3,499, by page count) and 4 "Ecommerce Website Packages" (RM3,899–RM6,799, by page count), each with specific feature sets (animated banners, CMS, SEO keyword counts, product catalogues, inventory/shipping/payment gateway for ecommerce, 8% SST exclusion, RM549/yr renewal).
- Flagged the conflict before building: SWOT's prices are far above this morning's RM500/RM1,500 quantity-focused tier. Asked the founder directly (undercut / match / layer above) rather than guessing on a real pricing decision. Founder chose: ship SWOT's exact prices now as placeholders ("I will change later"), package list is the priority; add Ecommerce as a new category (not folded into existing tiers).
- Shipped: replaced the Starter/Business tiers entirely with the 9-package SWOT-mirrored ladder (own copy/wording, not copied marketing text) in `src/data/pricing.json`; updated `pricing.ts`'s segment type to `'informative' | 'ecommerce' | 'growth'`; extracted the repeated card markup into a new `PricingCard.astro` component (was about to be duplicated a 3rd time); rebuilt `/pricing` into three grouped sections. Updated every place that referenced the old Starter/Business names or RM500/1,500 figures: `faq.json` turnaround answer, `GrantSolutions.astro`'s SME card copy, `grant-assistance-info-sheet.md`'s per-package grant math, and `memory/FINANCE.md`'s rate table.
- Logged this as an explicit **supersession** in `memory/DECISIONS.md` (not a silent overwrite) referencing the original RM500 decision, since the founder's own quantity-pricing call from this morning was being reversed by their own later instruction — kept both decisions visible with the reasoning for each.
- Flagged two things for later, not fixed now (out of scope of "ship the list first"): the new "10+ Pages" Informative package (RM3,499) overlaps in price/scope with the pre-existing "Launch" growth tier (from RM3,000) — unreconciled; and every SWOT-matched price is explicitly a placeholder the founder said they'd retune.
- Verified: `astro check` clean (0 errors), live HTTP fetch of `/pricing` confirmed all 9 package names and all 9 exact prices render, homepage still 200.

## 2026-08-09 — /cmo audit, then act on 2 of the findings
- Ran a CMO-lens audit of the whole site (not asked for by a specific role command until invoked as `/cmo`): checked for a positioning doc (none existed), proof content (still zero — same gap flagged earlier today), technical SEO basics (robots.txt/sitemap/JSON-LD all fine, but default OG image and self-hosted fonts are still empty per the content gate), and found two new issues by reading actual page code rather than just the rendered site: the footer newsletter signup form (`Footer.astro`) has `onsubmit="return false;"` and is completely non-functional — it silently discards every email address; and `/career` presented a fictional 3-person team, multiple open job positions, health insurance, and a "fully stocked kitchen at our KL workspace" despite every internal role doc describing WSS as a one-person company. Gave the founder a ranked list rather than fixing anything unprompted, since several of these (career page, positioning direction) are founder calls.
- Founder responded with two direct instructions: remove the career page, and build the positioning doc.
- **Career page removal:** deleted `src/pages/career.astro`, `src/data/career.json`, `src/data/career.ts`; removed both nav references in `src/data/nav.ts` (main nav "Company" group + footer company links); removed its collection from `public/wss-console/config.yml` (the CMS would otherwise still offer to edit a page that no longer exists). Verified `/career` now returns a clean 404 and grepped the full `src` tree to confirm zero remaining references. Logged as a decision (not just a task) since it was a real content/positioning call, not a bug fix.
- **Positioning doc:** auto-drafted `.agents/product-marketing.md` (the canonical location per the product-marketing skill) from the codebase — README, `pricing.json`, `services.json`, `site.json`, `about.astro`, the BizBrain role skill files (which state outright this is a one-person company), and this session's own history (SME pivot, SWOT-matched pricing, career removal). Covered the full template (overview, audience, competitive landscape vs. SWOT, differentiation, objections, switching dynamics, brand voice, proof points, goals) and added an extra "Open Issues" section — not in the skill's template, but worth keeping — that surfaces the three things a downstream marketing skill would otherwise get wrong if it just read the doc at face value: zero proof content, the About page's fictional team roster (found while drafting this — `about.astro` lists "Wan Syahiid," "Farah Izzati," and "Rin Takahara" as staff, same species of issue as the career page, not yet removed since the founder didn't ask for that one), and placeholder pricing. Added `memory/MARKETING.md` as the memory-system pointer to it, since `/cmo`'s own workflow checks that path first.
- Did NOT touch the About page team roster, the broken newsletter form, or any of the other audit findings beyond the two the founder explicitly named — all queued in `TASKS.md` with enough detail to act on later without re-deriving the reasoning.
- Verified: `astro check` clean (0 errors, file count dropped 79→77 matching the 2 deleted pages), dev server check confirmed `/career` 404s, `/` and `/about` still 200, footer no longer shows a Career link.

## 2026-08-10 — /memory recall found unlogged work, then a 20-item founder punch list

- Ran `/memory recall` at session start and found a large batch of **uncommitted, unlogged work** already sitting in the working tree (not from this session): 7 real blog posts added to `src/content/posts/` with honest `sourceUrl`/`sourceLabel` attribution, the About page's fictional 3-person team replaced with a real "Razali, Founder" placeholder frame, a fully-wired "Leave a review" modal on `/testimonials` (Web3Forms), a "Special Partnership Program" section, and reworked `contact.astro`/`faq.astro`/`insights` pages. Flagged the discrepancy to the founder rather than assuming it was mine or fully understanding its scope.
- Found and fixed the one real gap: the footer newsletter form (`Footer.astro`) still had `onsubmit="return false;"`, silently dropping every email. Wired it to Web3Forms using the same pattern as the other forms on the site (contact, hero quote, review modal), with loading/success/error states.
- Founder then sent a 20-item punch list (numbered, screenshot-referenced) covering homepage, contact, insights, about, testimonials, and FAQ pages, and asked to check what was already done vs. not. Read every relevant file's current (working-tree) state — Header, Hero, WhateverYouNeed, Differentiator, index, AboutSection, contact, about, faq, testimonials, insights/index, insights/[slug] — against each of the 20 asks.
- **Result: all 20 items were already implemented** in the uncommitted working tree (from the same earlier unlogged session), including some fairly involved builds: a hover-triggered navbar dropdown with a services grid box, the hero image replaced by a working free-quote form, homepage section reorder (grants block now sits directly above the FAQ teaser), the "Have a project in mind" CTA no longer rendered on the homepage, "Happy clients" already at 15+ (only one occurrence in the codebase, `Differentiator.astro`), the contact page's location card, subject field, and Reach-us relabel, and — the most elaborate one — an Insights click-to-open modal system: clicking a post title opens a popup with the full first-person article content plus an explicit "Visit the source" button, rather than navigating to a new page (individual `/insights/[slug]` pages still exist too, as an SEO-crawlable fallback).
- Verified every item against the *live rendered HTML*, not just the source: `astro check` (0 errors), dev server HTTP 200 on all 6 changed pages, and targeted string checks (`grep -c`/byte-offset order checks) confirming: "Have a project in mind", "Meet our expert team", "Discover More", "Still have questions", and "Call Center"/"X / Twitter" are all absent from their respective pages now; "Razali" and "Special Partnership Program" are present; the grant section's byte offset in the homepage HTML precedes the FAQ section's.
- Did not find anything to build from scratch for this punch list — the only code change this session was the newsletter form fix (see above). Told the founder plainly that most of the list was already done rather than re-implementing working features.
- Still open: none of this batch (blog posts, About/testimonials/contact/insights rework, newsletter form) is committed to git yet — it's all uncommitted working-tree state. Founder has not been asked yet whether to commit.

## 2026-08-14 — /dev: consolidated 14 service pages into 4 category pages
- Founder gave 4 new top-level category titles (Digital Consulting & Solutions, Web & Software/App Development, Digital Design & Media Marketing, Artificial Intelligence & Automation) and asked to combine the 14 individual `/services/[slug]` detail pages into 4 pages under those titles, deleting the 14 specific pages, while explicitly keeping the 14-card "Boost your brand with our expertise" section on `/services` (`ServicesGrid.astro`) untouched in content/design.
- Mapped all 14 services to the 4 categories (not asked to confirm first, given auto-mode bias toward proceeding — flagged to founder for correction if wrong): **Web & Software, App Development** = web-development, mobile-app-development, custom-systems, student-project-support (the old "Build" group, unchanged). **Artificial Intelligence & Automation** = ai-development only (the old "Intelligence" group, unchanged). **Digital Design & Media Marketing** = ui-ux-design, branding-services, product-photoshoot-marketing, media-creation, digital-marketing. **Digital Consulting & Solutions** = business-growth, grant-government-project-support, business-startup-package, maintenance-support.
- Built: new `src/data/serviceCategories.ts` (category → member-service-slugs map, plus a `serviceHref()` helper that resolves any service slug to its category page + anchor). Rewrote `src/pages/services/[slug].astro` so `getStaticPaths` now generates the 4 category pages instead of 14 service pages; each page stacks per-service modules (hero image, value prop, deliverables, approach, tools, pricing, FAQ — same content as the old individual pages) each with `id={service.slug}` for deep-linking, plus jump-links in the hero. `GrantSolutions` still renders only under the grant-support module.
- Updated every internal link that pointed at an old `/services/{slug}` page to `serviceHref(slug)` instead: `ServicesGrid.astro` (the maintained 14-card section — cards, images, and copy untouched, only the destination URL changed), `Header.astro`'s nav dropdown, and the homepage's separate `HomepageServices.astro` "Boost your brand" bento (3 hardcoded services).
- Added `public/_redirects` (Cloudflare Pages 301 redirect format) mapping all 14 old slugs to their new `/services/{category}#{service}` URL, so any bookmarked/indexed old links don't dead-end. **Known limitation:** this file is only honored by Cloudflare Pages at deploy time — `astro dev`/`astro preview` do not process it, so the old slugs will correctly 404 locally until it's actually deployed. Verified this is the expected/only gap, not a bug.
- Verified: `astro check` (0 errors), dev server checks — all 4 new category pages return 200 with the right services and anchor IDs present, `/services/web-development` (old slug) 404s locally as expected pre-deploy, `/services` still renders all 14 flip-cards with correct new hrefs, homepage links updated correctly.
- Not done / still open: none of this is committed to git yet (same uncommitted-working-tree situation as the rest of this project). The category→service mapping is my judgment call, not founder-confirmed — worth a quick look before shipping. `public/wss-console/config.yml`'s Decap CMS "Group" field (Build/Intelligence/Design & Care/Grow) was left as-is since it's a different, pre-existing taxonomy used for eyebrow/tone coloring, not the new page grouping — not touched.
- **Committed and pushed to origin/main** (commit 678a2b6, no Co-Authored-By trailer per founder request). Unrelated uncommitted admin-template deletions were deliberately left out of this commit — still sitting uncommitted, separate open item.
- **Same session, separate fix:** founder asked to remove Claude as a GitHub contributor. Cause: an older commit (a5d064e, 2026-07-11, "Reposition as a full digital solutions studio...") carried a Co-Authored-By: Claude trailer, which GitHub's contributor graph reads independently of any other commit. Push was initially blocked (push:false on founder's GitHub account for this repo, confirmed via `gh api`); founder added themselves as a collaborator, which fixed it. Rewrote that one commit's message via a non-interactive `git rebase -i` (GIT_SEQUENCE_EDITOR/GIT_EDITOR scripted, no manual editor) to strip the trailer, cascading new hashes through the 9 commits after it. Stashed and restored the unrelated uncommitted admin-template changes around the rebase so they weren't disturbed. Verified the diff between local and origin was exactly the intended change before force-pushing with --force-with-lease.
- **Same session, follow-up:** founder asked to change the nav dropdown too (screenshot showed the old 10-item truncated service list under "Popular services"). Swapped `Header.astro`'s dropdown to list the 4 categories instead, single column so full titles don't truncate.

## 2026-08-14 — /uiux: matte grey + red/yellow theme enhancement pass
- Founder asked (via `/uiux`) to add matte grey, red, and yellow to the theme and "make it more better," plus a broader "enhance my entire website" ask. Read the existing token system (`src/styles/global.css`) first: a light, ink-first theme with cobalt blue (`--color-accent`) as the sole primary interactive color, blue/red/yellow/green used only as categorical service-group tags (`lib/tone.ts`), and `paper-dim`/`paper-deep`/`line` carrying a faint blue cast rather than true neutral grey.
- Decision (not asked to confirm first, given auto-mode bias — logged for founder review): kept blue as the primary interactive accent (buttons/links/focus rings are used too pervasively sitewide to safely swap in one pass) and instead (a) retinted the core neutral surface tokens toward true matte grey, which cascades through every page automatically since those tokens back nearly all section banding, and (b) gave red and yellow real, deliberate presence in specific high-visibility spots rather than diluting blue everywhere. Full rationale and token table written to new `docs/DESIGN.md` (didn't exist before — created per the uiux skill's "this file is law for /dev" instruction).
- Token changes (`global.css`): `--color-paper-dim`/`--color-paper-deep`/`--color-line` retinted from blue-grey to matte grey; added a `--color-matte`/`-dim`/`-soft` neutral scale; added `--color-accent-yellow-vivid` (true saturated gold, for flat decorative fills only — kept the original darker `--color-accent-yellow` for text/labels since it's the one that passes AA contrast on white).
- Applied red/yellow deliberately: pricing highlighted/recommended tier switched from blue to red on both `/pricing` (`PricingCard.astro`) and the `/services` plan-preview cards (badge, ring, CTA); homepage `Differentiator.astro`'s 2x2 icon tiles now cycle blue-red-yellow-green instead of flat blue x4; `Hero.astro` (both variants) gained a second low-opacity background blob (yellow-vivid / red) opposite the existing blue one; `Testimonials.astro`'s mark-icon color cycle had blue listed twice (silently dropping red) — fixed to red-blue-green-yellow.
- Also fixed, found while doing this: `src/pages/services/[slug].astro`'s `toneAccent` map had `red: 'bg-accent'` (blue!) instead of `'bg-accent-red'` — meant the AI Development category page's icon tiles rendered blue despite its "Intelligence" tag correctly showing red everywhere else. Real pre-existing bug, not something introduced by the Aug 14 services-consolidation work (same wrong mapping existed in the original per-service page too).
- Verified: `astro check` (0 errors), dev server 200s across `/`, `/pricing`, `/services`, `/services/artificial-intelligence-automation`, `/about`, `/testimonials`, `/contact`; grepped rendered HTML to confirm `bg-accent-red` appears on the pricing highlight tier and `accent-yellow-vivid`/retinted `paper-dim` are present in the compiled output.
- Not done / explicitly deferred (flagged in `docs/DESIGN.md`'s "Next candidates"): extending the same per-tile color-cycling pattern to other icon-grid sections (`Capabilities.astro`, `WhateverYouNeed.astro`); no dark-mode variant was requested or built. "Enhance my entire website" was interpreted as this color-system pass, not a full layout/typography/motion rebuild — worth clarifying with the founder if they wanted more than that.

## 2026-08-14 — three quick founder-driven edits (mid-session interrupts)
- **Removed the "Process FAQ" section from `/process`** (screenshot showed 6 Q&As: typical timeline, ownership, handover, documentation, post-launch support, sign-offs). Deleted the `<FAQ>` render call, its inline `processFaq` data array, and the now-unused `FAQ` import from `src/pages/process.astro`. Verified: 0 errors, "Process FAQ" absent from rendered HTML.
- **Homepage nav bar is now transparent** (screenshot showed the floating pill nav's white background looking heavy over the hero). `Header.astro`'s floating pill (`bg-paper/85`) now conditionally drops to `bg-transparent` only when `currentPath === '/'`; every other page keeps the white/85%-opacity pill with blur, since their heroes are shorter/less busy behind the nav. Verified both states in rendered HTML (`bg-transparent` on `/`, `bg-paper/85` on `/about`).
- **Removed the 3-card "Build It / Grow It / Launch It" grid from the homepage** (`WhateverYouNeed.astro`), per founder's screenshot — kept the section wrapper and its "Whatever it is, we probably build it" heading, replaced the cards with a dashed-border image placeholder labeled "Gambar laptop, komputer, mobile app yang ada real design" (Bahasa Malaysia — founder said the real image will come later from their media designer). Verified the old card text is gone and the placeholder renders.

## 2026-08-14 — /cfo+/dev: replaced the 12-package pricing grid with 5 stage-based packages
- Founder sent a screenshot of the full 12-tier pricing grid (5 Informative + 4 Ecommerce SWOT-matched packages + Launch/Build/Enterprise growth tier — the SWOT-mirrored structure from the 2026-08-09 supersession) and said to remove it entirely, replacing it with exactly 5 packages built around **business maturity stage** instead of page-count/ecommerce-tier, explicitly combining services from the 14-service/4-category system built earlier this session. Founder gave detailed audience descriptions per tier but left exact service bundles, feature lists, and pricing to be filled in ("make it details").
- Named the 5 packages **Launch → Expand → Grow → Scale → Custom** (founder used "Launch"/"Package A"/"Package B"/"Package C"/"Custom" as working labels; gave Package A/B/C real names consistent with Launch/Custom to read as one coherent ladder — not confirmed with founder, flagged for review):
  - **Launch** (RM 1,200) — brand-new businesses, zero prior digital presence (hawker stalls, home-based traders). Just `web-development`. Note explicitly warns MADANI grant needs 6+ months SSM registration, so most Launch clients won't qualify yet (a real eligibility nuance, not just boilerplate).
  - **Expand** (RM 4,500) — 1+ year in, modestly profitable, ready to invest. `web-development` + `digital-marketing` + `branding-services`. Grant-eligible.
  - **Grow** (RM 12,000, marked Most Popular) — SME 3+ years, wants redesign/revamp + social/analytics marketing, no in-house developer. `web-development` + `ui-ux-design` + `custom-systems` + `digital-marketing` + `maintenance-support`. Includes 3 months Care Plan. Grant-eligible.
  - **Scale** (From RM 28,000) — corporate, multiple staff, new systems + analytics + automation + integrations. `custom-systems` + `web-development` + `ai-development` + `digital-marketing` + `business-growth` + `maintenance-support`.
  - **Custom** (Custom Quote) — big/grant-funded/AI-ML projects. `ai-development` + `custom-systems` + `grant-government-project-support` + `business-growth`.
  - Prices are my placeholders, not founder-confirmed — same "ship the structure first, retune price later" pattern the founder used on 2026-08-09 for the SWOT tiers. Flagged in TASKS.md.
- Built: rewrote `src/data/pricing.json` (5 tiers, each with a new `audience` and `services` field in addition to the existing schema); extended `PricingTier` type in `pricing.ts` (`segment` now `'launch'|'expand'|'grow'|'scale'|'custom'` instead of `'informative'|'ecommerce'|'growth'`); added the missing `segment`/`audience`/`services`/`note` fields to the Decap CMS schema in `public/wss-console/config.yml` (pre-existing gap — `note` wasn't editable via CMS even before this change); rewrote `/pricing` as a single 5-card section instead of 3 grouped-by-segment sections; surfaced the new `tier.audience` field as a small label on `PricingCard.astro`; updated the FAQ turnaround-time answer (`faq.json`) to reference the new package names instead of the old Informative/Ecommerce/Launch/Build/Enterprise ones.
- Verified: `astro check` (0 errors), dev server 200s on `/`, `/pricing`, `/services`; grepped rendered HTML to confirm all 5 package names render on both `/pricing` and the `/services` plan-preview grid, "Most popular" badge present once (on Grow), and no leftover references anywhere in `src` to the old package names or segment values.
- Not done: the 5-package naming (Launch/Expand/Grow/Scale/Custom) and all pricing figures are unconfirmed — flagged in TASKS.md for founder review before this goes live. The now-orphaned `grant-assistance-info-sheet.md` admin template (already deleted from disk per the earlier-flagged uncommitted deletion) previously had per-package grant math tied to the old 9-tier structure — worth noting if that template gets restored, its numbers will need a full rewrite against the new 5 tiers.

## 2026-08-14 — /uiux: full theme rebrand to "Industrial Concrete" (business card + raw-concrete mood board)
- Founder asked where matte grey was showing up (answered: mostly subtle surface tokens, one unused neutral scale), then said they wanted the entire site's style/theme changed to match their business card, sent it as a PDF, and separately sent 6 reference images of raw-concrete/brutalist architecture (exposed structure, board-formed concrete, industrial stairwells) plus a Google Images search link for "industrial building theme grey concrete Tamarind Square" — explicitly naming Tamarind Square Cyberjaya as the target vibe. Founder's own words: "no need to over maintain the light theme, change what necessary, prioritize my dream" — read as genuine full creative latitude, not a request to check in first.
- Read the business card directly rather than guessing: red is the dominant/first-read color (logo mark, "RAZALI" name, a large red graphic), dark charcoal/near-black is the secondary structural color ("Devlabs" wordmark, dark UI), light grey with visible photographic grain is the base surface, and blue appears only as a single thin underline stripe and small icon circles — clearly a minor accent, not primary. This directly contradicted the *previous* same-day design decision (see DECISIONS.md matte-grey entry) which deliberately kept blue as the primary interactive accent — explicitly superseded that call given the new, much more authoritative brand reference.
- Decision (not asked to confirm first, per the founder's explicit "prioritize my dream" latitude — logged for review): promoted red to the primary interactive accent (`--color-accent` itself, not just the categorical `--color-accent-red`) so it drives every button, link, focus ring, hover glow, and CTA band sitewide from one token; demoted blue to categorical/stripe-only use; shifted the entire light-theme neutral scale from white/navy-tinted to a warmer, greyer "cast concrete" palette (base `--color-paper` is no longer pure white); added a sitewide film-grain texture wash (`body::after`, fixed, 3.5% opacity, overlay blend, reusing the fractal-noise SVG the codebase already had as an opt-in utility) so every surface reads as textured material rather than flat digital color — this was the single highest-leverage way to deliver "grainy" across the whole site without touching every component file.
- Also found and fixed: several buttons (`Button.astro`, `Hero.astro`, `QuoteForm.astro`, `testimonials.astro`, `services/[slug].astro`) had their glow-shadow color hardcoded as a literal blue rgb value (`shadow-[...rgb(45_91_227...)]`) rather than referencing the accent token, so the token swap alone wouldn't have caught them — a red button would've kept a blue glow. Retinted all of them to match.
- Explicitly did NOT do (flagged in `docs/DESIGN.md`'s "Next candidates," not silently skipped): a border-radius/corner-sharpness pass (the mood board is all sharp-edged concrete; this site is still fully rounded-corner, `rounded-3xl`/`rounded-full` everywhere — matching that literally would mean touching dozens of files, judged as bigger/riskier than the color+texture change and not explicitly asked for); no layout/composition changes (no structural graphic motifs, hero still uses the existing soft blurred-blob decoration); no typography change (kept Geist/Outfit/Space Grotesk — already fairly geometric/technical, didn't seem like what the founder was pointing at).
- Rewrote `docs/DESIGN.md` from scratch as "Industrial Concrete," explicitly marked as superseding the earlier same-day "matte grey, blue-primary" version, with the full palette table, the brief/reasoning, and an explicit changed-vs-not-changed section so a future session (or `/dev`) doesn't have to reverse-engineer which of today's two design passes is current.
- Verified: `astro check` (0 errors), dev server 200s across `/`, `/pricing`, `/services`, `/about`, `/contact`, `/testimonials`. Could not visually screenshot-verify (browser automation still blocked by the Windows Application Control policy on this machine, noted repeatedly across sessions) — founder should eyeball it live before treating this as final.
- Not done: this is a color/texture pass, not the deeper structural rebrand (corners, layout, graphic motifs) the mood board could support if the founder wants to go further — see `docs/DESIGN.md` "Next candidates."

## 2026-08-21 — /memory checkpoint: reconciling a week of unlogged work found in the repo

**This entry is a course-correction, not a continuation.** Running `/memory checkpoint` today surfaced that the actual repo state has moved far beyond what this file tracked, entirely outside this memory system. Per the memory skill's own rule ("memory files are the truth; chat recollection is not... discrepancy gets flagged"), documenting that gap here rather than silently appending as if nothing happened.

- **What was found:** `git log`/`git reflog` show 11 commits after my last logged one (`678a2b6`, the services-consolidation push), dated 2026-08-14 22:20 through 2026-08-17 21:33, all committed under the founder's own git identity (`rxzalii`). None of them are logged anywhere in `memory/`. They include: "update color," "package update," an "SME market entry blueprint" adding brand-new pages (`express.astro`, `marketplace/`, `status.astro`), a full navigation paradigm change ("convert top nav to left side nav bar," "implement minimalist floating icon side rail navigation," with follow-up sizing/positioning fixes), removal of the "Malaysian brand trust grid" and "Direct Builder Philosophy" footer bar, a `/work` page overhaul ("interactive prototype viewer, flagship client builds, sprint methodology"), and a final "update to light off-white theme, add date-time scheduling, new hero contact form, and 3-pillar package cards."
- **What happened to my own uncommitted "Industrial Concrete" work (the last thing I did before this gap):** partially carried forward, partially superseded — not simply lost, but not intact either. `--color-accent` is still red (`#d92d20`), the sitewide grain wash and neutral (non-navy) ink are still present, and my services-consolidation/5-package-pricing work (`serviceCategories.ts`, `pricing.json`'s "Launch" tier) is intact. But the concrete-grey background I set (`--color-paper: #f2f0eb`) is now `#fcfbfa` ("crisp light off-white" per its own code comment — a deliberate reversal of "no more pure white"), and the sharpened corner-radius scale I set (`--radius-3xl: 0.625rem`) is back to the original soft `1.5rem`. Reads as later, deliberate design iteration on top of/instead of my pass, not an accident — but it happened with zero memory trail, so this is a best-effort reconstruction from `git log`, not a first-hand account.
- **⚠️ Real concern, not just a logging gap:** the current `docs/DESIGN.md` was fully rewritten (title now "WSS DevLabs — Master Design & Visual Architecture Specification") and credits two named "Lead Specialists" — **"Natasya Umairah" (Front-End, UI/UX & Micro-Moments)** and **"Razin Hakim" (Full-Stack & Backend Architecture)** — as real team members driving design decisions throughout the document. `memory/CLIENT.md` (unchanged, still accurate per every other session) states plainly: *"Owner/founder: rxzalii ... running WSS DevLabs as a one-person digital agency."* This is the same species of issue this project has hit and fixed multiple times before — fabricated testimonials (2026-08-09), a fictional About-page team (found 2026-08-09, not yet removed as of that session), a fictional career-page team (removed 2026-08-09) — all logged as real content-integrity problems in this file's own history. Did not touch it this session; flagging for the founder to confirm whether these are real hires, a deliberate placeholder/persona device, or another instance of the same recurring mistake.
- **Not investigated this session** (checkpoint only, no fixes made): whether the new `express.astro`/`marketplace/`/`status.astro` pages and the new side-nav-rail component follow the project's content gate (no fabricated proof content); whether the old top-nav `Header.astro` I edited earlier this session still exists/is still used, or was fully replaced by the new side-rail nav; whether `date-time scheduling` and the `new hero contact form` mentioned in the last commit message duplicate or replace the quote-form work from earlier sessions.
- **Next step:** founder should confirm (a) whether the "Natasya"/"Razin" names in `docs/DESIGN.md` are real and should stay, or are fabricated and should go, and (b) whether future work on this project should assume the newer Aug 15–17 direction (light off-white, side-nav) as current, superseding this session's "Industrial Concrete" dark/red/sharp-corner direction, since both currently exist in tension.

## 2026-08-14 — /wssdevlabs → /uiux: finished the "Industrial Concrete" follow-through
- Founder responded to the deferred "Next candidates" list with explicit permission via `/wssdevlabs`: "revised my design.md to this industrial design.. you get my permission, do it. /uiux proceed to entire redesign." Treated as continuation of already-locked `docs/DESIGN.md` direction (Phase 6 already done), not a new pipeline — went straight to execution rather than re-running intake/quote/requirements/etc., since this is an internal project already deep in build, not a new client engagement.
- **Sharpened the border-radius scale globally** by overriding Tailwind v4's default `--radius-xs` through `--radius-4xl` theme tokens in `global.css` (confirmed the exact default values by reading `node_modules/tailwindcss/theme.css` first, rather than guessing) — roughly a 6-8x reduction across the board (e.g. `--radius-3xl` 1.5rem → 0.625rem). Because the whole codebase already used Tailwind's scale-based `rounded-xl`/`2xl`/`3xl` utilities (confirmed via grep — 60+ occurrences across 30+ files) rather than one-off arbitrary values, this single token edit reshaped every card, panel, image, and section corner sitewide with zero per-file changes — the same "one token, cascades everywhere" leverage used for the color pass. Separately fixed the small number of things the token override couldn't reach: hardcoded arbitrary values (`rounded-[2rem]`/`rounded-[2.5rem]` in 4 files) and literal `border-radius` declarations in the `.card`/`.panel`/`.panel-ink`/`.panel-charcoal`/`.glass-float-card` component classes in `global.css` itself. Deliberately left `rounded-full` (all pill buttons/badges) untouched — it's outside the numeric scale entirely, and keeping pills soft against otherwise sharp panels was a considered choice, not a gap.
- **Added structural/architectural motifs to `Hero.astro`**: extended the existing `.grid-pattern` (blueprint-style fine line grid) to the inner-page hero variant (previously home-page-only); added one thin diagonal accent line to the home hero styled like a concrete beam edge crossing the frame; replaced a redundant second blur-blob (home hero had two blobs that became the same red after the earlier accent-token swap) with a neutral matte-grey blob instead of introducing another bright color, keeping "mostly grey, one red signal" intact; retinted `.grid-pattern`/`.dot-pattern` from the old navy `rgb(12 18 32)` to the new neutral ink `rgb(25 25 27)` so the blueprint-grid actually matches the new ink color.
- Rewrote the relevant sections of `docs/DESIGN.md` to move these from "Next candidates" to "done," keeping only the genuine dark-mode-toggle idea as still-deferred (judged as real theme infrastructure, a bigger and separate undertaking, not implied by anything said this session).
- Verified: `astro check` (0 errors), dev server 200s across all 10 main routes (`/`, `/pricing`, `/services`, `/about`, `/contact`, `/testimonials`, `/work`, `/insights`, `/faq`, `/process`). Same caveat as the color pass: no visual/screenshot verification possible (browser automation still blocked on this machine) — founder should eyeball live before calling this final, especially the sharpened corners since that's the boldest single change.

## 2026-08-22 — /uiux + /dev: resolved the fabricated-team question, page-by-page live edit session, homepage/nav/marketplace/services overhaul
- **The long-open fabricated-name question is resolved.** Founder confirmed directly: "remove the razin and others, maintain only if there is name razali only" — the founder's real name is Razali. Removed every "Razin Hakim" / "Natasya Umairah" reference site-wide (Header.astro nav copy x4, `data/express.ts`, `data/status.ts` incl. the `directBuilders` array collapsed from 2 fake people to one real "Razali" entry, `pages/express.astro`'s two-photo "Direct Builder" section rewritten to one honest founder card, `pages/work/index.astro`, `pages/marketplace/index.astro`, `components/sections/SMEProjectCalculator.astro`, and `docs/DESIGN.md`'s "Lead Specialists" credit line) and rewrote `data/team.json`/`team.ts` to a single real Razali entry instead of the two fake ones. Also deleted the fabricated 6-person "Meet Our Experts" `TeamGrid.astro` homepage section (Razin/Natasya plus 4 more invented names with stock headshots) per explicit founder instruction.
- **Found and fixed a second, more serious instance of the same bug class**: `components/sections/Testimonials.astro` had its own hardcoded 2 fake testimonials (fake names, fake companies, stock headshots) and **completely ignored its own `testimonials` prop** — so even though `services/[slug].astro` correctly passed the real (empty) `TESTIMONIALS` array, the component silently rendered the fake ones anyway on every service page. Rewrote it to accept the prop, default to the real data source, and render nothing when empty (matches the project's existing content-gate pattern). `pages/testimonials.astro` was already doing this correctly — but its per-review photo was `picsum.photos/seed/wss-review-{i}`, i.e. a fake photo that would attach to any real reviewer's name the moment one gets added; swapped for an initial-avatar placeholder.
- **Removed stock photos standing in for real people/team/office across the site** (founder's direction: "cut fake stock photos... startup company, too much photo for now"): Hero.astro homepage headshot, HomepageServices.astro "collaboration" photo, About page's 3 team/office photos + hero banner photo, ElevateCTA "senior architect" portrait, Differentiator "team at work" photo, QuoteForm "consultant" photo, contact.astro's fake office photo, coming-soon.astro's fake workspace photo, and 3 Header.astro mega-menu thumbnails that paired a stock photo with "architect"/"Razali" copy — all replaced with abstract pattern/blob panels or icon placeholders, no stock photography implying real people.
- **Marketplace trimmed from 15 systems to 6** (`data/marketplace.ts`) per founder request ("i only want 6 things... choose the easiest"): kept the 6 whose slugs were already hardcoded into `pages/work/index.astro`'s flagship-build case studies (`wss-eventspace`, `wss-medcare`, `wss-ticketflow`, `wss-buildright`, `wss-directory`, `wss-staffportal`) so no cross-links broke — removed `wss-forms`, `wss-flexbuilder`, `wss-tablereserve`, `wss-salonflow`, `wss-easyrent`, `wss-primeproperty`, `wss-shopflow`, `wss-gatedvault`, `wss-careerhub`. Updated every "15 Systems"/"15 Buy-Once PHP Systems" reference in `Header.astro` to "6". Header's marketplace mega-menu sub-links had been pointing to slugs that never existed in any version of `marketplace.ts` (`clinic-queue-emr`, `service-booking-engine`, etc.) — fixed to real slugs while trimming.
- **Removed `/services/bespoke`** (the standalone Bespoke Enterprise Engineering page) per founder request, with a 301 redirect (`public/_redirects`) to `/services`; updated all internal links (`Header.astro` x2, `data/nav.ts` x2, `HomepageServices.astro`) that pointed to it.
- **Added a 5th services pillar: "IoT Solutions & Infrastructure"** (`data/serviceCategories.ts`, slug `iot-solutions-infrastructure`) alongside the existing 4 (Web & Software, Digital Design & Media Marketing, Digital Consulting & Solutions, AI & Automation) per founder's explicit "5 core services" list. It has no `services:[]` entries yet (nothing in the 14-card `SERVICES`/`ServicesGrid.astro` maps to it — founder explicitly said to leave the 14-card grid on `/services` untouched), so its category page currently renders hero + cross-links + CTA only, no per-service modules — honest-empty rather than fabricated.
- **Header/nav full redesign**: converted the fixed left icon rail into a floating top bar (founder: "change this to top bar navbar..maintain the icon" → later "floating rounded corner nav bar" → later "remove the white background" → later "make the nav bar floating in hero section for all page at first section"). Final state: a fully transparent (no bg/border/shadow/blur) icon row fixed at the top of every page, overlaying the hero directly (no reserved top padding in `BaseLayout.astro`), using the founder's real logo (`public/images/brand/logo.png`, now also the browser favicon) sized to the bar. Along the way, fixed real nav bugs found during the rebuild: 4 sub-links that all pointed to the identical `/status` URL and 3 that all pointed to the identical `/express#packages` URL (added real anchor ids — `#metrics`/`#builders`/`#pipeline`/`#deployments` on `status.astro`, `#express-landing`/`#express-starter`/`#express-business` on `express.astro` — and corrected the Express tier names/prices in the nav copy, which didn't match the real RM180/RM1,500/RM4,500 packages). `NAV_GROUPS`/`FOOTER_SOLUTIONS_LINKS` in `data/nav.ts` also lost their Bespoke entries.
- **Contact-form duplication fixed** (founder: "dont put same contact form in multiple pages..limit it to 2"): `QuoteForm.astro` had been dropped identically onto `/pricing`, `/services`, and all 4 `/services/[slug]` category pages with no per-page customization — and `[slug].astro` was stacking a `CTABand` directly above it, i.e. two consecutive contact prompts on every service page. Kept `QuoteForm` on exactly 2 pages (`/contact` — its own separate hand-built form — and `/pricing`); every other page now ends in a single `CTABand` CTA instead.
- **Misc site-wide changes**: swapped GitHub social links for TikTok everywhere (`data/site.json/.ts`, `Footer.astro`, `coming-soon.astro`, CMS `config.yml`, `data/team.ts/.json`) per founder request; `.badge-highlight` (the colored pill wrapping key words in headings) changed from black to red (`--color-accent`) with a heartbeat pulse animation per founder request; removed the small "✦ + label" eyebrow micro-headings from 5 homepage sections and made the shared `Eyebrow.astro` component a no-op (used in 9+ files) per "remove text like this for all section in all pages"; floating WhatsApp button relabeled to a "Consult with Us" pill then reverted back to icon-only per founder's follow-up, with the WhatsApp glyph swapped for a generic chat-bubble icon ("i will put ai chatbot later").
- Verified throughout: `astro check` after every batch (0 errors each time), and a full route smoke-test (24 URLs incl. all 6 marketplace product pages and all 5 service category pages) all returning 200 at the end.
- **Committed and pushed** to `origin/main` (`435f86e`) per explicit founder instruction ("after finish..push all").
- **Not done / still open**: the founder also asked for (a) scroll-reveal/card-reveal/"3D transition" motion to make the site "feel alive," and (b) a general typographic-hierarchy strengthening pass — founder chose "stronger hierarchy, same system" over literal multiple fonts/colors when asked to clarify. Neither has been started; flagged as the natural next task.
- **New question for the founder**: with the desktop nav now a fully transparent overlay with dark icons and no background, contrast may be poor on the two pages with dark (`bg-ink`) hero sections — `/express` and `/status` — since founder asked for this treatment "for all page at first section" without seeing those two live yet. Worth a founder check once they view those two pages.

## 2026-08-22 — /uiux + /dev: continued live page-by-page session (nav settled, light-theme hero rollout, homepage pillar relabel)
- **Nav bar went through several rapid live iterations** before settling: transparent overlay (as pushed) → founder asked to "rewind" (`git checkout` on `Header.astro`/`BaseLayout.astro`, which correctly restored the actually-pushed transparent version — I'd mis-described it mid-session as a bordered pill, corrected once caught) → founder then explicitly asked for the rounded-corner **glass** pill back ("floating in the 1st section... glass nav bar... dont let the section content visible inside"), landing on: `rounded-[2.5rem]` pill, `bg-white/15` + `backdrop-blur-2xl` + `border-white/40`, no reserved top padding (floats directly over each hero). This is the final, current state.
- **Every page's first section got extra top clearance** (`about`, `express`, `status`, `marketplace` index/detail, `work` index/detail, `services` index/detail, `pricing`, `faq`, `contact`, `testimonials`, `insights` index/detail) so headings don't collide with the floating glass nav — done per-page (not via the shared `<main>` padding) since the nav itself has no reserved space.
- **`/express`, `/marketplace`, `/status` hero sections rebuilt from dark (`bg-ink`) to the homepage's light theme** — same wording throughout, `grid-pattern` + soft blobs background, light pill eyebrow badge, `.badge-highlight` red box wrapping the key phrase in each `<h1>`, light stat/metrics bar replacing the dark translucent one. Founder is doing this hero-by-hero, not all at once — same treatment should be offered for any other dark-hero page found later.
- **Homepage's 5-pill "Our Services" list relabeled to exactly match the 5 real category names/hrefs** (Web/System/Mobile App Development, Digital Consulting & Solutions, Digital Design & Media Marketing, AI & Automation, IoT Solutions & Infrastructure) — previously this list mixed marketplace/express product links in with service-category links; now it's purely the 5-pillar overview linking to the 5 real `/services/{category}` pages.
- **Resolved a direct self-contradiction from the founder**: earlier in this same session founder said to leave the 14-card `ServicesGrid.astro` untouched ("maintain this 14 card"); later in this session asked to "add new card for IOT Solutions and Infrastructure" pointing at that exact grid. Flagged the contradiction via AskUserQuestion rather than guessing; founder confirmed "add it here" — so a **15th real service** (`iot-development`, real deliverables/pricing RM 8,000/FAQ, not a placeholder) was added to `services.json` and wired into the IoT category page's `services: []` array, which was previously intentionally empty.
- **About page decluttered further**: removed the dark charcoal "About Us" title banner+breadcrumb entirely, removed the "ABOUT US" eyebrow label, removed the whole "Step-by-step to your growth" 3-step process section. The page's only remaining `<h1>` is now "One team, whatever the digital need" (promoted from h2, since removing the banner removed the page's only heading).
- **Footer recolored to matte charcoal** (`bg-charcoal`, was light `bg-paper-dim`) to match the logo per founder request, with every text/link/social/WhatsApp-button color re-paired for contrast on dark. Founder then asked for the real logo image in place of the text+bar brand mark in the footer — since the logo's wordmark is dark and the footer is now dark too, wrapped it in a small white chip so it stays legible (founder's request didn't mention this, my judgment call to avoid a contrast bug).
- **Pricing card grid fixed** (`/services` "Plans that scale with you" preview): with only 4 tiers left (after Scale's removal) in a 3-column grid, "Custom" wrapped alone onto a short narrow row — founder asked to widen it to the right edge and shorten it. Now `sm:col-span-3` with a horizontal (row) layout on larger screens instead of the vertical stack the other 3 use. Also fixed a real bug this surfaced: the 3 remaining standard cards' price boxes weren't aligned to the same row across the grid because the inner content div had lost its `flex-1` during the Custom-tier refactor — restored.
- Trimmed several more flagged micro-copy/CTA elements across `Hero`, `GrantSolutions`, `MarketplaceAndExpressBanner`, `ElevateCTA`, `SMEProjectCalculator`, `express.astro` hero (badge + CTA row), `services/index.astro` (bottom link paragraph + `ServicesGrid`'s "Learn more" card link), `marketplace/index.astro` hero badge — all one-off "remove this" instructions from founder screenshots, not systematic passes.
- Verified: `astro check` 0 errors after every batch; committed and pushed to `origin/main` (`6632372`) per founder's "push" instruction.
- **Still not started**: scroll-reveal/3D "feel alive" motion pass and the typography-hierarchy strengthening pass (both flagged in the previous entry, still open — founder has stayed focused on page-by-page content/layout fixes instead).
- **New open item**: `/marketplace/[slug]` (individual product pages) and `/work/index.astro` still have dark `bg-ink` heroes — founder has been doing the light-theme hero conversion one page at a time by sending screenshots; these two haven't come up yet but are the same pattern as `/express`/`/marketplace`/`/status`.
